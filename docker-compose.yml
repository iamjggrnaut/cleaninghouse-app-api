version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: cleaninghouse-postgres
    environment:
      POSTGRES_DB: cleaninghouse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: React2022
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
